# Application settings
DEBUG=false

# CORS settings (comma-separated origins)
# CORS_ORIGINS=http://localhost:3000,https://ttmm.vercel.app

# File upload settings
MAX_FILE_SIZE_MB=10

# Chunking defaults
DEFAULT_CHUNK_SIZE=1000
DEFAULT_CHUNK_OVERLAP=200
